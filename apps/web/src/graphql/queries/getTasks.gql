query GetTasks($filter: TaskFilterInput, $skip: Int, $take: Int) {
  tasks(filter: $filter, skip: $skip, take: $take) {
    id
    title
    description
    status
    priority
    deadline
    createdAt
  }
}
