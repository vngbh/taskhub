{
  "name": "taskhub",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "seed": "turbo run seed"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "turbo": "^2.8.21",
    "typescript": "^6.0.2"
  }
}
